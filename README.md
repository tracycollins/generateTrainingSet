# generateTrainingSet
Generate neural network training + test sets of categorized twitter users

For each manually categorized twitter user in the database:
- update user info (tweets, language analysis, histograms
- add user to archive

Creates archive (.zip) of data sets and maxInputHashMap
