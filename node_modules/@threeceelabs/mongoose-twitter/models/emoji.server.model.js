const mongoose = require("mongoose");
mongoose.Promise = global.Promise;
const Schema = mongoose.Schema;

exports.EmojiSchema = new Schema({
	nodeId: { type: String, unique: true },  // image -> :emoji: unEmoji
	nodeType: { type: String, default: "emoji" },
	emojiId: { type: String },  // image -> :emoji: unEmoji
	text: { type: String },  // raw emoji
	createdAt: { type: Date, default: Date.now },
	lastSeen: { type: Date, default: Date.now },
	lastTweetId: { type: String },
	mentions: { type: Number, default: 0 }
});